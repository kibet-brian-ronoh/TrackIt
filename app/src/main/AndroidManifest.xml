<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trackit">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HelpInstallTrackingDevice"></activity>
        <activity android:name=".MyTrackingDevice" />
        <activity
            android:name=".ReportStolen"
            android:parentActivityName=".LocationMap" />
        <activity android:name=".Settings" />
        <activity android:name=".MyCar" />
        <activity android:name=".LocationHistory" />
        <activity android:name=".Me" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LocationMap"
            android:label="@string/title_activity_location_map" />
        <activity android:name=".TrackingDeviceDetails" />
        <activity android:name=".VehicleDetails" />
        <activity android:name=".CreateAccount2" />
        <activity android:name=".LogIn"
            android:parentActivityName=".CreateAccount"/>
        <activity android:name=".CreateAccount" />
        <activity android:name=".SplashPage" />
    </application>

</manifest>